apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "locanta-fe.fullname" . }}-test-connection"
  labels:
    {{- include "locanta-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "locanta-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
